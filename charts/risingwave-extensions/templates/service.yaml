apiVersion: v1
kind: Service
metadata:
  name: {{ include "risingwave-extensions.fullname" . }}
  labels:
    {{- include "risingwave-extensions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "risingwave-extensions.selectorLabels" . | nindent 4 }}
